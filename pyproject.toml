[build-system]
requires = [
    "setuptools>=45",
    "wheel",
    "setuptools_scm>=6.2"
]

[tool.setuptools_scm]

[tool.black]
line-length = 120
skip-string-normalization = true
target-version = ['py37']
